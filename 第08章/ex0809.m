%ex0809.m 求解离散系统单位脉冲响应
clear,clc  %清内存中
A=[1,-0.7,-0.6,1]; %A系数向量
B=[1];  %B系数向量
n=[-20:100]; %作图的脉冲响应长度
x=[n==0];  %输入脉冲 等价于x=[zeros(1,20),1,zeros(1,100)];
y=filter(B,A,x); %求得脉冲响应
stem(n,y);  %作图
title('脉冲响应');
grid
